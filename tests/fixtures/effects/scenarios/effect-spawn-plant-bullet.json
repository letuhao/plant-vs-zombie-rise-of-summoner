{
  "id": "effect-spawn-plant-bullet",
  "seed": 42,
  "steps": [
    { "op": "clear" },
    {
      "op": "grant",
      "grant": {
        "grantId": "live-spawn-pb",
        "effectId": "fx.spawn_plant_bullet",
        "ownerKey": "match",
        "overlay": { "icd_ms": 0 }
      }
    },
    { "op": "hit" },
    {
      "op": "expectPlan",
      "expect": {
        "contractVersion": 2,
        "trigger": "OnDamageDealt",
        "actions": [
          {
            "seq": 1,
            "action": "SpawnEntity",
            "effectId": "fx.spawn_plant_bullet",
            "grantId": "live-spawn-pb",
            "params": { "kind": "plant", "typeId": 0 }
          },
          {
            "seq": 2,
            "action": "SpawnEntity",
            "effectId": "fx.spawn_plant_bullet",
            "grantId": "live-spawn-pb",
            "params": { "kind": "bullet", "typeId": 0 }
          }
        ]
      }
    }
  ]
}
