{
  "schemaVersion": 1,
  "version": 1,
  "_meta": {
    "owner": "docs/architecture/demon-seed/spec-species-effects.md",
    "note": "Working values, not a validated balance decision. poolAffinityWeightMilli turns the model's 'likely'/'occasional' judgement into a weight (a model may never write the weight itself, spec §3); fixedCoreBandByRarity is a SEPARATE band from ssot-rarity.md §3.3's own affix-count band — the fixed core carries its own, smaller band so a rung-1 species can still have exactly one defining effect (spec §4).",
    "rebalance": "Never hand-edit by convention alone — this file has no publish tool yet, a direct edit here is a real diff, bump `version`."
  },
  "poolAffinityWeightMilli": {
    "likely": 700,
    "occasional": 300
  },
  "fixedCoreBandByRarity": {
    "chaff": { "min": 0, "max": 0 },
    "sprout": { "min": 0, "max": 1 },
    "grafted": { "min": 0, "max": 1 },
    "cultivated": { "min": 1, "max": 1 },
    "fused": { "min": 1, "max": 2 },
    "chimeric": { "min": 1, "max": 2 },
    "heirloom": { "min": 1, "max": 2 },
    "firstseed": { "min": 2, "max": 2 },
    "sunwoven": { "min": 2, "max": 3 },
    "almanac": { "min": 2, "max": 3 }
  }
}
