# The derived-stat sheet — 269 channels, five open families, and six states

**Status:** Detail design, 2026-08-23. **Document 9 of 9** owed by
[gap-audit-2026-08-22.md](gap-audit-2026-08-22.md) §7 (gap **A26**), built second because it is the
heaviest consumer of [spec-magnitude-and-units.md](spec-magnitude-and-units.md) and shares its evidence.

**Closes** the `Channel · Panel` cell, which [00-foundation.html §G](00-foundation.html) marked `n/a`.
That `n/a` was wrong: a channel absolutely has a panel — it is the thing a player opens to find out
why their creature hits harder than it did an hour ago.

**Sources, all read this session:**
[actor-hub-ssot.md §3, §6.1, §11](../architecture/actor-hub-ssot.md) ·
[`DerivedStatRegistry.cs`](../../src/FusionRpg.Core/Stats/Derived/DerivedStatRegistry.cs) ·
[`DerivedStatChannels.cs`](../../src/FusionRpg.Core/Stats/Derived/DerivedStatChannels.cs) ·
[`DerivedComposer.cs`](../../src/FusionRpg.Core/Stats/Derived/DerivedComposer.cs) ·
[`ActorDerivedSnapshot.cs`](../../src/FusionRpg.Core/Stats/Derived/ActorDerivedSnapshot.cs) ·
[`DerivedTurnChannels.cs`](../../src/FusionRpg.Core/Battle/Timeline/DerivedTurnChannels.cs) ·
[status-ssot.md §9](../architecture/status-ssot.md).

---

## 1. What is actually there — counted, not quoted

**Updated 2026-08-24 (element-families, T3.1) for the derived-stats program's H.1–H.7 extension
(catalog-extension, T2) — same hardcoded-count defect as element-hub-ssot.md §6, same fix: drift-tested
against the generator, not hand-recomputed and left to go stale again.**

| Group | Count | Source |
|---|---:|---|
| `combat.*` — **28 families × (omni + 6 elements)** | **196** | generated by `BuildAllCombatChannelIds()`, [DerivedStatChannels.cs](../../src/FusionRpg.Core/Stats/Derived/DerivedStatChannels.cs) (12 original + 16 from H.1); asserted derived, not literal, in [DerivedStatRegistryTests.cs](../../tests/FusionRpg.Core.Tests/ActorHub/DerivedStatRegistryTests.cs) |
| `status.power.*` / `status.resist.*` constants | 8 | `RegisterDefaults()`, [DerivedStatRegistry.cs](../../src/FusionRpg.Core/Stats/Derived/DerivedStatRegistry.cs) |
| `status.duration.*` / `status.durationReduction.*` / `status.intensity.*` / `status.intensityReduction.*` constants (H.2, T2) | 16 | `RegisterNonElementExtensions()`, same file |
| `progression.*` — 5 bonus + power/realm + H.7's `xpRate`/`breakthroughSuccess` | 9 | same |
| `combat.heal.power` (H.4, T2) | 1 | same |
| `resource.max/regen/efficiency.*` (H.5, T2 + `poise` 2026-08-26) | 18 | same — six resource ids × 3 channels |
| `move.range` (H.6, T2) | 1 | same |
| `skill.cooldown/effectiveness.*` (H.3, T2) | 10 | same |
| `loadout.slots` (H.8, D4.25 — party-dungeon, spec-unique-pipeline.md §4) | 1 | same |
| **Pre-registered total** | **269** | 99 → 256 (T2) → 259 (class-system `poise-resource`, 2026-08-26) → 261 (P0.5 / battle-timeline B9, `turn.speed`+`turn.haste`, 2026-08-28) → 267 (action-corpus Phase 0.8, 2026-09-02: `combat.heal.power` generalised into `resource.restore.{resource}` — +6 members, and the old id stays registered as a retirement shim so archived `aptitudes.v1/v2/v3.json` remain loadable) → 268 (party-dungeon D4.25, 2026-09-06: `loadout.slots`, the extend-action-slot grant's derived channel) → **269** (base-defense siege-ai, 2026-09-07: `ai.aggression`, §5.20 rule 4's signed targeting-priority scalar) |
| Nine **open-ended prefix families** — `status.power.{id}` · `status.resist.{id}` · `status.duration.{id}` · `status.durationReduction.{id}` · `status.intensity.{id}` · `status.intensityReduction.{id}` · `status.immune.{tag}` · `status.immuneReduction.{tag}` · `status.expose.{category}` | **unbounded** | resolved dynamically in `TryResolveChannel` (5 original + 4 from H.2, T2) |
| …of which the locked **24**-status catalog could expand the first **six** by | **+144** | [status-ssot.md §9](../architecture/status-ssot.md) — 24 statuses × 6 sparse-eligible dimensions (was 21 → +126 before `nerve.*`) |

**So the sheet must render ~385 channels today (was ~141) and cannot know the ceiling.** That single
fact rules out every design that enumerates channels in the component. The sheet renders **what the
snapshot holds**, laid out by a rule, not by a list.

---

## 2. The structural insight — the combat block is a matrix, not a list

One-hundred-ninety-six channels is unreadable as a list and trivially readable as a grid, because they
are not 196 things. They are **28 families × 7 element slots**, generated as exactly that:

```csharp
foreach (var family in CombatChannelFamilies) {          // 28 (12 original + 16 from H.1)
    ids.Add($"{family}.{ElementRoster.OmniId}");         // + omni
    foreach (var element in ElementRoster.Concrete)      // + 6
        ids.Add($"{family}.{element.ToElementId()}");
}
```

The UI must have the same shape as the generator, for the same reason the generator has it: **adding an
element or a family changes the grid by construction.** A hand-authored layout would need editing on
every roster change, and the roster has already changed once (light/dark, 2026-08-21) and the family
list twice (the four `combat.shield.*` families, then H.1's sixteen).

| | omni | fire | ice | air | earth | light | dark |
|---|---|---|---|---|---|---|---|
| power · defense · crit rate · crit resist · crit damage · crit resist damage · accuracy · dodge · shield capacity · shield toughness · shield pen · shield regen | | | | | | | |

**Row order is the generator's order, not alphabetical** — offense families, then defense, then shield.
Sorting the rows by name would separate `crit.rate` from `crit.resist`, which are read as a pair.

### The omni column is not a seventh element

`omni` is an **additive baseline**, not a type — it adds into every element's effective total, and it is
never an actor type slot ([element-hub-ssot.md §4, §7](../architecture/element-hub-ssot.md)). The grid
renders it **first and visually separated** by a rule, so it never reads as "the seventh element". A
picker choosing an actor's element type must not offer it at all.

---

## 3. Six states, because zero is four different things

This is the part a naive sheet gets wrong. A channel showing `0` today may mean any of these, and they
demand different player responses:

| State | Means | Renders |
|---|---|---|
| **`active`** | non-zero, with contributions | the value, in its unit class |
| **`default`** | registered, nothing writes it, sitting at its default | the default, dimmed. **Not blank** — the channel is real and gearable |
| **`capped`** | at its registry cap — **the next point does nothing** | value + a `CAP` marker and the cap. Never the bare number |
| **`stub`** | the value is a hardcoded placeholder, not a computed one | the value + *"placeholder — the real curve is not built"* |
| **`no-producer`** | registered and readable, but nothing in the game can currently write it | *"nothing grants this yet"* |
| **`unregistered`** | the id exists in code but the registry **rejects** it | developer surfaces only, flagged red |

**`capped` is the one that is a correctness issue, not a nicety.** `status.resist.dot`, `.cc` and
`.contagion` cap at **0.95** ([DerivedStatRegistry.cs:46-48](../../src/FusionRpg.Core/Stats/Derived/DerivedStatRegistry.cs),
`Cap()` at `:135`), and `status.immune.*` / `status.immuneReduction.*` cap at **1**. A player at the cap
who stacks more resist gets **literally nothing**, and a sheet printing `0.95` with no marker has told
them to keep buying. Note the asymmetry, which is deliberate and must survive into the UI:
**`status.resist.omni` has no cap at all** ([actor-hub-ssot.md §3.D](../architecture/actor-hub-ssot.md)
— *"omni uncapped"*), so the omni cell and the category cells in the same row behave differently.

### The three states that exist right now, named

Verified in code this session, so the sheet ships with real examples rather than hypotheticals:

| Channel | State | Evidence |
|---|---|---|
| `progression.power`, `progression.realm` | **`stub`** | default `1.0` via `FlatReplace`, [DerivedStatRegistry.cs](../../src/FusionRpg.Core/Stats/Derived/DerivedStatRegistry.cs); `ActorDerivedSnapshot.StubNeutral()` hardcodes both to `1.0`. `TierPower = power × realm` = 1.0 for every actor in the game |
| `progression.bonus.arm1`, `.arm2` | **`no-producer`** | registered and read, no writer |
| `status.expose.{category}` | **`no-producer`** | resolves dynamically; **zero readers** |
| `turn.speed`, `turn.haste` | **`active` / registered** | Registered in `RegisterDefaults()` (battle-timeline B9, 2026-08-28) — FlatSum with non-zero defaults. **Errata 2026-09-10:** earlier text called these unregistered |
| `turn.moveSpeed` | **`unregistered`** | Declared in [DerivedTurnChannels.cs](../../src/FusionRpg.Core/Battle/Timeline/DerivedTurnChannels.cs); still **not** in `RegisterDefaults()` |

> **Correction (2026-09-10).** `turn.speed` / `turn.haste` are registered. Only `turn.moveSpeed`
> remains the unregistered example for §3 `unregistered` state demos.

**Show unchanged (policy lock D4, derived-cook 2026-09-10):** the toggle hides rows in state
**`default` only**. Rows in **`no-producer`** stay visible with fiction copy (*"nothing grants this
yet"*) — they must not be collapsed into “unchanged.” Shipped FE that hid both is defective.

**These four states are the `Pending<T>` contract, applied to a stat.** `absent` and `pending` looking
identical is the exact bug the sealed contract exists to prevent
([game-gui-map.md §Contract](../architecture/game-gui-map.md)), and a stat sheet is where a player is
most likely to meet it: *"my fire resist is 0"* and *"fire resist does not do anything yet"* are
different sentences and must look different.

---

## 4. Compose kind is player-facing, in plain words

Four kinds fold contributions differently
([DerivedComposer.cs:38-70](../../src/FusionRpg.Core/Stats/Derived/DerivedComposer.cs)), and the
difference is visible to anyone who adds a second source. **The term of art never ships; the behaviour
sentence does.**

| Kind | What it does | The line the player reads |
|---|---|---|
| `FlatSum` | `default + Σ Flat` | *"Sources add together."* |
| `FlatReplace` | highest `Priority` **wins outright**; ties broken ordinal by `SourceId`; only if no `Replace` exists does it sum flats | *"The strongest source wins — these do not add."* |
| `SumIncreased` | `default + Σ Increased`, **then capped** | *"Sources add, up to the cap."* |
| `MaxPriorityFlag` | `max()` over flag-ish mods | *"On or off — the strongest source decides."* |

**`FlatReplace` is the one that must be labelled**, because it is the only kind where the contribution
list does *not* sum to the total. A detail view listing three sources under a total that equals just one
of them looks like a bug unless the sheet says why. This affects `progression.power` and
`progression.realm` — the two `TierPower` inputs — so it is on the path of every status apply.

---

## 5. The three levels

Same ladder discipline as everywhere else: each level is built from the one below.

### 5.1 Summary — on the actor card, not the sheet

Six to eight lines, chosen by **what is non-default on this actor**, never a fixed list. Ends with a
*"see all N"* affordance carrying the real count. A fixed summary would hide the one channel a player's
build actually moved.

### 5.2 The sheet — a band-2 panel (cook primary rail)

**Shipped IA (gui-lego `derived-console`, 2026-09):** primary tabs are **Elements · Status ·
Resources · Other** with a **variant rail** (element / status-category / action-category / Shared).
That cook rail is the **player chrome SSOT**. The 28×7 combat **matrix** remains the structural
truth of the combat block (Guard 1) and may appear as element occupancy pips / Elements-tab
presentation — it is **not** a competing primary tablist of sheetGroups (offense/pools/…).

```text
┌ Derived · Emberling ──── [search] [Show unchanged] ─────────────────────────────┐
│  Elements | Status | Resources | Other     ← cook primary                        │
│  Omni | Fire | Ice | …                     ← variant rail (expand mode)          │
│  family blocks → channel rows → inspect                                          │
└──────────────────────────────────────────────────────────────────────────────────┘
```

**Zero-suppressed by default, with `Show unchanged` as a real toggle — not a hidden default.**
Hide **`default` only** (D4). The count of what is hidden is always visible, so suppression never
reads as absence. **`no-producer` stays visible.**

**The unit label is on the group/family row, never in the cell** — this is
[spec-magnitude-and-units.md §3](spec-magnitude-and-units.md)'s rule expressed as a layout constraint,
and it is what stops `+150 crit rate` and `+12 fire power` being read as comparable. Guard 3 of that
document tests exactly this.

### 5.2a Player names — runtime catalog, not `idWords` (added 2026-09-07; home amended same day)

The sheet in §5.2 already used English family names (*Power*, *Crit rate*). The FE did not:
`channelLabel` in `web/fusion-rpg-web/src/contract/adapt.ts` title-cases the dotted id because
`catalog.json` authors no `displayName`. That is a named gap, not a style miss.

**Home (amended 2026-09-07):** [`data/tuning/derived-stat-catalog.v{n}.json`](../../data/tuning/) —
families carry `displayName`, `reading`, `icon`, `gauge`, sheet group, and a cap **ref**. Cap
*values* stay in `data/tuning/derived-stats.v{n}.json` (`categoryResistCap`, `turnDefaultSpeed`).
Seed [`lexicon.v1.json`](../../data/seed/derived-stats/lexicon.v1.json) and
[`catalog.json`](../../data/seed/derived-stats/catalog.json) become check mirrors once hosts inject
the tuning catalog. The sheet still renders **what the snapshot holds**, laid out by the catalog
rule — not by a hardcoded component list (§1).

A player row binds `(displayName, reading, unitClass, gauge, cap)` from the catalog plus the six
render states from §3. `idWords` is developer-only (GG-62).

### 5.2b StatRow + InspectSplit (added 2026-09-07; cook rail note 2026-09-10)

The combat block is still a **matrix** (28 × 7) at the data layer. Under cook IA, the player meets
it as **variant × family list** (Elements tab + element variant), not 196 bare `<tr>` of ids.
Occupancy pips / sparks are **`derived-statrow-gauge` (D7 deferred)** — optional polish after
six-state + CAP truth land.

| Piece | Job |
|---|---|
| **channel-row / StatRow** | icon · catalog name · number · optional spark · select. One expanded channel per row |
| **Cook grouping** | Primary tab + variant (not sheetGroup as primary chrome) |
| **InspectSplit** | clicking a row fills the **right inspector** (value, unit sentence, compose sentence from §4, cap or “no cap — more still counts”, contribution list). Not a nested dialog (GG-63) |

**Spark policy** is the catalog `gauge` field when D7 lands. Uncapped `GameUnits` sparks are *relative to this
actor’s siblings* and never paint `CAP`. Registry caps (`status.resist.dot` at 0.95) fill against
the cap and show the §3 marker. That is GG-64 / PS-8, not a third classification.

The ActorSheet **Derived** tab *is* this sheet. Harden program: [derived-cook-map.md](../architecture/derived-cook-map.md).

### 5.3 Channel detail — the "why"

Opened from a StatRow into the inspector (same panel). Shows: the value, its unit class, the compose sentence from §4, the cap and distance
to it, and **the contribution list — each source named in the fiction's words**, never `sourceId`.

This is where the player answers *"where did this come from"*, and it is the only place the sheet can
honestly claim to explain a number.

> **What it cannot show yet, and must say so.** Four producers write derived channels today with **no
> registered subsystem row** — patron, stars, injuries, and contracts
> ([actor-hub-ssot.md §6.1](../architecture/actor-hub-ssot.md)). The `stat.derived` kind **has landed**;
> until those four producers adopt it, their contributions arrive **unattributed**. The detail view renders those as
> *"from your pact"* / *"from an injury"* where the producer is knowable and **an explicit
> `unattributed` row where it is not** — never silently folded into the total, which would make the
> list not add up with no explanation.

---

## 6. Open-ended families

The sheet cannot enumerate `status.power.{id}` ahead of time, and must not try.

| Rule | |
|---|---|
| **Render what the snapshot holds** | `ActorDerivedSnapshot.Channels` is the truth ([:35](../../src/FusionRpg.Core/Stats/Derived/ActorDerivedSnapshot.cs)) |
| **Plus the known expansion** | the 21 locked statuses × 2 families = 42 rows, shown as `default` when absent, because those are gearable and a player should know they exist |
| **Group `{statusId}` rows under their status**, not their family | a player thinks *"my Wither"*, not *"my status.power"* |
| **An unknown `{id}` still renders** | a channel arriving from a future status shows with its raw id in a neutral row rather than being dropped. **Dropping is the one unacceptable behaviour** — it makes a real bonus invisible |
| **`{tag}` flag families render as tags, never numbers** | `Immune to Freeze`, per the `Flag` unit class |

The component therefore binds to **`(channelId, value, unitClass, composeKind, cap, state)`** and has no
knowledge of which channels exist — the same registry-shape discipline the resource meter uses.

---

## 7. Guards

| # | Guard | Fails when |
|---|---|---|
| 1 | **The grid is generated, not authored** — row × column count equals `CombatChannelFamilies.Count × (ElementRoster.Concrete.Count + 1)` | someone hand-lists channels; breaks the moment an element or family is added |
| 2 | **Every rendered channel carries a state from §3** | a `0` ships without saying which kind of zero it is |
| 3 | **A capped channel at its cap renders the cap marker** | a player is told to keep stacking a dead stat |
| 4 | **No numeric column mixes unit classes** | inherits [spec-magnitude-and-units.md §8](spec-magnitude-and-units.md) guard 3 |
| 5 | **An unknown channel id renders rather than throwing or dropping** | a future status's channel silently vanishes |
| 6 | **`FlatReplace` channels render the compose sentence** | a contribution list does not sum to its total, with no explanation |
| 7 | **Volume fixture at current §1 scale (~269 registered + open-prefix expand; ~385 today when status catalog expands sparse dims) and at 500 channels (stress)** | the panel is only ever tested against a near-empty actor. GG-50. Numbers follow §1 — do not hand-recompute independently |

---

## 8. What this changes elsewhere

| File | Change |
|---|---|
| [00-foundation.html §G](00-foundation.html) | `Channel · Panel` moves from `n/a` to **D.5**. The `n/a` was a wrong refusal, not a deliberate one |
| [00-foundation.html](00-foundation.html) | new **§D.5** — the sheet, the six states, the 12 × 7 grid |
| [actor-hub-ssot.md §11.4](../architecture/actor-hub-ssot.md) | two unregistered `turn.*` channels → **three** |
| [information-architecture.md](information-architecture.md) | the sheet is reachable from the Actor panel, band 2. It is **not** a tenth rail entry — it is opened from a creature, which is GG-4's *"going to look"* test |

---

## 9. Design-gate checklist

```
[x] I identified the subsystem(s) this touches — stats/derived, status, element, battle timeline.
[x] I read every doc in the §1 row(s) this session: DESIGN-GATE.md, actor-hub-ssot.md §3/§6.1/§11,
    element-hub-ssot.md §4/§6/§7, status-ssot.md §9, plus the five source files listed at the top.
[x] I checked decisions.md for a lock covering this (Game GUI row; Resource model row for §3.G).
[x] Every factual claim cites file:line.
[x] I verified claims against CODE, not comments. The 84 count is read from the generator and its
    test; the six states are read from RegisterDefaults/TryResolveChannel/Cap; the turn.* gap was
    re-tested rather than taken from the SSOT — and the SSOT's count was wrong by one.
[x] I read the surrounding section of every rule I quoted.
[~] I tested (not assumed) any constraint I am reporting. PARTIAL: no test suite was run. Every
    claim here is a read of source or a grep. The turn.* claim is "RegisterDefaults does not
    register them and TryResolveChannel has no turn. branch" — read, not executed.
[x] Nothing contradicts a §2 invariant. §3.G resource channels are explicitly NOT designed here;
    they are unregistered and an ADR gates them.
[~] Corrections propagated. Plate 00 §D.5 + §G land with this document. actor-hub-ssot.md §11.4
    is corrected in the same pass.
```
