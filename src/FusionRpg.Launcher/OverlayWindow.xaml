<Window x:Class="FusionRpg.Launcher.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Rise of Summoner — Overlay"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" ShowActivated="True"
        Background="#FF101418">
  <Grid>
    <wv2:WebView2 x:Name="Web" />
    <TextBlock x:Name="FallbackText"
               Visibility="Collapsed"
               Margin="40" TextWrapping="Wrap"
               FontSize="16" Foreground="#FFE0E0E0"
               VerticalAlignment="Center" HorizontalAlignment="Center" />
    <Button x:Name="BackButton"
            Click="Back_Click"
            HorizontalAlignment="Right" VerticalAlignment="Top"
            Margin="0,10,10,0" Padding="10,4"
            Opacity="0.85"
            Background="#CC202830" Foreground="#FFE8F0E8" BorderBrush="#66FFFFFF" />
  </Grid>
</Window>
