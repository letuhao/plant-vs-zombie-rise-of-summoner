namespace FusionRpg.Injector;

/// <summary>
/// In-game F8 IMGUI cheat menu — retired.
/// Web FE at the RPG server URL is the only cheat UI (SSOT).
/// Full former source kept as CheatMenuGui.Legacy.cs.bak (not compiled).
/// </summary>
public static class CheatMenuGui
{
    public static void Draw()
    {
        // no-op
    }
}
