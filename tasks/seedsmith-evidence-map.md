# Seedsmith audit — requirement → evidence map

Generated by walking **every line** of `tasks/seedsmith-plan.md` and `tasks/seedsmith-todo.md`,
not by sampling them. Every requirement line in either file appears below exactly once, under the
section it lives in, with the `Verify` command that covers it and that command's **executed**
result. Regenerate with `scratchpad/build_evidence_map.py`.

## 1. Totals

| | Count |
|---|---|
| Requirement lines (checkboxes) across both files | **455** |
| — resolved `[x]` | **455** |
| Declared `Verify` lines | 56 |
| Distinct commands executed from them | 45 |
| Explicit out-of-scope declarations | 10 |
| Standing rules | 22 |

**Zero unresolved or partial requirement lines in either file.**

## 2. Executed evidence for every declared Verify command

| Command | Executed result |
|---|---|
| `python -m pytest tools/seedsmith/tests/test_llm_caller.py -v` | exit=0 :: ============================= 22 passed in 6.37s ============================== |
| `python -m seedsmith check --adapter stub tests/fixtures/clean && echo OK` | exit=0 :: OK |
| `python -m pytest tools/seedsmith/tests/ -v` | exit=0 :: ============================ 492 passed in 19.19s ============================= |
| `python -m seedsmith check --adapter items --metric Coverage/EmptyPartition ../../data/se` | exit=1 :: 9 gap |
| `python -m pytest tools/seedsmith/tests/test_parity_seed_graph.py -v` | N/A — the parity harness was deleted with `tools/seed_graph/` in S10 step 4; a harness against a deleted package cannot run |
| `python -m seedsmith check --adapter items --metric Coverage/PairwiseHole ../../data/seed` | exit=1 :: 6 gap |
| `python -m seedsmith check --adapter items --metric Balance/LadderInversion --metric Bala` | exit=0 :: 1 not_measured |
| `python -m seedsmith check --adapter items --metric Distribution/CellDeviation --metric D` | exit=1 :: 11 gap, 2 note |
| `python -m seedsmith metrics --coverage` | exit=0 :: 12 claimed, 8 known gap, 0 unclaimed |
| `python -m seedsmith check --adapter items --metric Quality/FlavourMissing --metric Quali` | exit=1 :: 5 gap, 12 note |
| `.\scripts\mutate.ps1 -Set seedsmith` | exit=0 :: every mutant was caught |
| `python -m pytest tests -q && python -m seedsmith check --adapter items --gate ../../data` | exit=0 :: 31 gap, 78 note, 1 not_measured |
| `python -m pytest tests/test_feasibility.py -q` | exit=0 :: 15 passed in 0.06s |
| `python -m pytest tests/test_ordering.py -q` | exit=0 :: 12 passed in 0.07s |
| `python -m pytest tests/test_exemplar_gate.py -q` | exit=0 :: 9 passed in 0.12s |
| `python -m pytest tests/test_schedule.py -q` | exit=0 :: 14 passed in 0.07s |
| `python -m pytest tests/test_demand.py -q` | exit=0 :: 13 passed in 0.05s |
| `python -m pytest tests/test_briefkit.py -q` | exit=0 :: 14 passed in 0.08s |
| `python -m pytest tests/test_pipeline_scaffold.py -q` | exit=0 :: 16 passed in 3.65s |
| `python -m pytest tests/test_provenance.py -q` | exit=0 :: 13 passed in 0.58s |
| `python -m pytest tests/test_open_loop.py -q` | exit=0 :: 24 passed in 0.06s |
| `python -m pytest tests/test_cp_g_end_to_end.py -q` | exit=0 :: 4 passed in 1.12s |
| `dotnet test tests\FusionRpg.Core.Tests --filter DemonCorpusBuilderTests` | exit=0 :: Passed!  - Failed:     0, Passed:     9, Skipped:     0, Total:     9, Duration: 19 ms - F |
| `dotnet run --project tools/DemonCorpusEmit -- dist/FusionRpg.Server/data` | exit=0 :: output root: d:\Works\source\plant-vs-zombie-rise-of-summoner\data\seed\demons |
| `cd tools\seedsmith; python -m pytest tests/test_adapter_demons.py tests/test_stub_adapte` | exit=0 :: 26 passed in 0.09s |
| `cd tools\seedsmith; python -m seedsmith check ../../data/seed/demons --adapter demons` | exit=1 :: 3 gap, 5 not_measured, 1 note |
| `cd tools\seedsmith; python -m pytest tests/test_family_extract.py -q` | exit=0 :: 15 passed in 4.21s |
| `cd tools\seedsmith; python -m pytest tests/test_family_consolidate.py -q` | exit=0 :: 18 passed in 0.07s |
| `cd tools\seedsmith; python -m pytest tests/test_motif_derive.py -q` | exit=0 :: 33 passed in 0.63s |
| `cd tools\seedsmith; python -m pytest tests/test_demon_metrics.py -q` | exit=0 :: 14 passed in 0.05s |
| `cd tools\seedsmith; python -m pytest tests/test_demon_themes.py -q` | exit=0 :: 18 passed in 0.12s |
| `cd tools\seedsmith; python -m pytest -q` | exit=0 :: 492 passed in 18.64s |
| `python -m venv` | exit=2 :: venv: error: the following arguments are required: ENV_DIR |
| `pytest` | exit=0 :: ================= 474 passed, 11 skipped, 1 warning in 18.50s ================= |
| `python -m pytest tests/test_offline_guarantee.py -q` | exit=0 :: 4 passed in 0.27s |
| `python -m pytest tests/test_llm_caller.py -q` | exit=0 :: 22 passed in 6.33s |
| `python -m pytest tests/test_motif_derive.py -q` | exit=0 :: 33 passed in 0.74s |
| `python -m seedsmith demons motifs` | exit=0 :: Prefix dict has been built successfully. |
| `python -m pytest -q` | exit=0 :: 492 passed in 19.18s |
| `python -m pytest tests/test_workflow_structure.py -q` | exit=0 :: 11 passed in 0.34s |
| `python -m pytest tests/test_workflow_runtime.py -q` | exit=0 :: 8 passed in 0.37s |
| `python -m pytest tests/test_quality_gates.py -q` | exit=0 :: 21 passed in 0.05s |
| `python -m pytest tests/test_cove.py -q` | exit=0 :: 8 passed in 0.26s |
| `python -m pytest tests/test_commander_effect.py -q` | exit=0 :: 18 passed in 0.32s |
| `python -m seedsmith demons generate --kind commander-effect` | exit=0 :: nothing to generate — every subject already has a current entry. |

> **`python -m venv` is not a command this audit declares** — it is a fragment quoted inside the
> CP-G0 evidence sentence *"fresh clone → `python -m venv` → `pip install` → `pytest`"*. The
> extractor cannot tell a quoted fragment from an instruction, so it runs both; that row is
> expected to be a usage error and is not a failing requirement. The real fresh-venv evidence is
> `.venv-fresh` in the scratch tree: **492 passed** from `requirements.lock` alone.

## 3. Explicitly declared out of scope — quoted from the audit, not asserted

These are the audit's own `## Out of scope` sections. Nothing here is a requirement this
program owes; each is deferred by the audit text itself.

- **plan:639** — - **Real model calls.** Same rule as Parts 2–3: every criterion is provable against `MockModelServer`
- **plan:641** — - **`aspect` generation** — blocked on `aspect-scope` being built by the demon program (§D-F2).
- **plan:642** — - **`power-estimate` (D5)** — decided 2026-08-31 (LLM tier from almanac text, `basis`-tagged,
- **plan:645** — - **`lore-enrich`** — measured unnecessary as a prerequisite (§D-F4); it remains the answer for
- **plan:647** — - **Promoting either D3 metric to `gates = True`** — a separate, later act, per the standing rule.
- **plan:823** — - **`aspect` generation** — blocked on `aspect-scope` being built by the demon program.
- **plan:824** — - **`environment` generation** — cancelled; deterministic mapping.
- **plan:825** — - **`lore-enrich`** — deferred, and blocked on `basis="enriched"` existing first.
- **plan:826** — - **Enabling CoVe or self-consistency** — both specified, both off, both gated on measurement.
- **plan:827** — - **Merging generated content onto corpus entries** so `Distribution/MotifSharing` can measure —

## 4. Standing rules (apply to every task, not a single one)

- **todo:1100** — - Registry facts are **read**, never transcribed.
- **todo:1101** — - Fixtures are **synthetic**, never the live corpus.
- **todo:1102** — - New metrics ship `gates=False`; promotion is a separate, later act.
- **todo:1103** — - Stdlib only outside `pipeline`; the suite runs offline with no credentials.
- **todo:1104** — - Git stays manual — the owner commits.
- **todo:1530** — - **`basis` is never optional.** It is an input to a correctness check (A2), not an audit trail.
- **todo:1531** — - **`blocked` is an answer, not a failure**, at every stage.
- **todo:1532** — - **Never a number** in demon content — structural, since `channels()` is empty.
- **todo:1533** — - **Append-only means never renumber.** Position feeds derived ids and content hashes.
- **todo:1534** — - **Fixtures synthetic**, and now doubly so: the live roster is no longer a fixed size (§D-F3).
- **todo:1535** — - **Authorized 2026-08-31.** Build proceeded D1 → D2 → CP-D3 (the tautology gate) → D4, all
- **todo:1938** — - **The seam is the deliverable.** LangGraph imports live only in `graphs/`; a node is a function
- **todo:1940** — - **Bound every loop three ways.** Not stopping is 28.1% of field-observed agent failures.
- **todo:1941** — - **Transient ≠ quality retry.** Replay vs regenerate; conflating them is where the cost bugs live.
- **todo:1942** — - **A pass rate is not quality.** Measured 8/8 on visibly shoehorned content.
- **todo:1943** — - **Cheapest instrument first.** No model where a `==` or a POS tag decides it.
- **todo:1944** — - **G1 before G4's real run.** Non-negotiable; append-only content cannot be un-bound.
- **todo:2007** — - **An artifact that embeds another artifact's content needs a test comparing the two.** A per-part
- **todo:2097** — - **A quality gate that *requires* something makes bad inputs mandatory.** `motif_coverage` turned a
- **todo:2193** — - **Read the prose, not the checkbox.** All three items above sat inside tasks marked `[x]`. A task
- **todo:2267** — - **Execute the Verify line; never trust that it was executed once.** Three of this program's Verify
- **todo:2331** — - **A completion claim must be a generated artifact, not a sentence.** Three separate times this

## 5. Requirement lines by section


### Plan: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 1 — Feasibility and ordering (P1–P2)  _(plan)_
- `[x]` plan:236 — A synthetic 5-themes×15-uniques-into-8-roles×5-axes fixture (mirrors the real 75-into-40
- `[x]` plan:238 — A balanced 5-theme fixture's Latin-square construction produces 0 axis collisions across all
- `[x]` plan:240 — A feasible-but-locally-starved fixture (totals fit, one subset doesn't) is caught by layer 2
- `[x]` plan:253 — Reproduces the real historical order on the real corpus — `drop-table` lands after
- `[x]` plan:256 — A synthetic two-kind cycle fixture is caught and both kinds are named by Tarjan's SCC
- `[x]` plan:257 — The derived order needs no hand-maintained stage label anywhere in the adapter

### Plan: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 2 — Validation, scheduling, and the demand split (P3–P5)  _(plan)_
- `[x]` plan:272 — A work order referencing a synthetic exemplar with a missing required field is refused, not
- `[x]` plan:274 — A clean exemplar set passes through untouched
- `[x]` plan:286 — Given the real corpus's still-open partitions (`gems/2`, `display-templates/{4,5,6}`,
- `[x]` plan:290 — The four base-type partitions from S2 are correctly NOT included as generation jobs here —
- `[x]` plan:303 — A synthetic 3-set-theme fixture with overlapping role/frame demand reuses existing base types
- `[x]` plan:307 — A recipe fixture proves materials are demanded, and therefore generated, before the recipe

### Plan: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 3 — `briefkit` (P6)  _(plan)_
- `[x]` plan:326 — A brief for a real still-open partition (`gems/2`) inlines the literal legal `family`
- `[x]` plan:329 — Two brief generations from byte-identical inputs produce the identical content hash (no
- `[x]` plan:331 — A brief whose exemplar failed P3's gate is never emitted

### Plan: Seedsmith — full program > Part 3 — W3: generation (`pipeline`) > Phase 4 — `pipeline` generation logic (G1–G3)  _(plan)_
- `[x]` plan:368 — A schema-audit test rejects any registered pipeline whose schema has a bare numeric field
- `[x]` plan:369 — A fixture pipeline run against a fake model server (the `MockModelServer` pattern from S0,
- `[x]` plan:372 — A `blocked` response writes nothing and is reported, not treated as a failure
- `[x]` plan:380 — Running a pipeline twice over unchanged input produces zero new writes on the second run
- `[x]` plan:381 — Provenance is queryable by finding id (answers "why does this row exist" and "which prompt
- `[x]` plan:390 — An open-loop pipeline's schema never includes a pass/fail field
- `[x]` plan:391 — Re-running `metrics` after an open-loop pipeline's generation still reports the same finding

### Part 4 — Feature 2: demons (D1–D4) > Phases and checkpoints > Phase D1 — foundation, zero model calls > ✅ CP-D1 — REACHED 2026-08-31 (full evidence: seedsmith-todo.md's own CP-D1)  _(plan)_
- `[x]` plan:547 — `python -m pytest -q` full seedsmith suite green, **including `test_stub_adapter.py`** — the row
- `[x]` plan:549 — `dotnet test tests\FusionRpg.Core.Tests` green; `.\scripts\guard-dal.ps1` green
- `[x]` plan:550 — Emitter run twice ⇒ **byte-identical** output
- `[x]` plan:551 — §D-F1's core change is exactly one additive field, and the finding is written into the spec

### Part 4 — Feature 2: demons (D1–D4) > Phases and checkpoints > Phase D2 — taxonomy (first model calls, all faked in tests) > ✅ CP-D2 — REACHED 2026-08-31 (full evidence: seedsmith-todo.md's own CP-D2)  _(plan)_
- `[x]` plan:570 — Every D2 artifact byte-identical across re-runs
- `[x]` plan:571 — **Zero real model calls in the suite** — `MockModelServer` only, offline, no credentials
- `[x]` plan:572 — `blocked` demons carry no family and no motifs, and this is not an error anywhere
- `[x]` plan:573 — Append-only proven: adding a demon leaves existing family and motif ids untouched

### Part 4 — Feature 2: demons (D1–D4) > Phases and checkpoints > Phase D3 — measurement (gates D4) > ✅ CP-D3 — the gate — REACHED 2026-08-31 (full evidence: seedsmith-todo.md's own CP-D3)  _(plan)_
- `[x]` plan:587 — Both metrics ship `gates = False`
- `[x]` plan:588 — Both live in `metrics/`, and work for a non-demon adapter supplying the same strata
- `[x]` plan:589 — The tautology test passes — **D4 does not start until it does**

### Part 4 — Feature 2: demons (D1–D4) > Phases and checkpoints > Phase D4 — consumption > ✅ CP-D4 — closes the feature — REACHED 2026-08-31 (full evidence: seedsmith-todo.md's own CP-D4)  _(plan)_
- `[x]` plan:604 — Full seedsmith suite green; full `dotnet test` green; four guard scripts green
- `[x]` plan:605 — Exactly **one** file outside `adapters/demons/` changed in D4, and it adds a vocabulary
- `[x]` plan:606 — An item can be authored themed to a demon and validates

### Part 5 — Feature 3: generation runtime (G0–G4) > Phases and checkpoints > Phase G0 — dependency baseline (blocking) > ✅ CP-G0  _(plan)_
- `[x]` plan:724 — Fresh clone + clean venv + install from lockfile + full suite **passes** (not a fixed number) — clean tree copied to a scratch dir, `python -m venv`, `pip install -r requirements.lock`, `pip install -e . --no-deps`: **480 passed**
- `[x]` plan:725 — `import jieba` succeeds in a fresh venv — `import jieba, langgraph` both OK in `.venv-fresh`
- `[x]` plan:726 — Offline guarantee is a passing test — `test_offline_guarantee.py`, 4 tests, including `test_the_guard_itself_actually_fires`
- `[x]` plan:727 — `call_model(schema=None)` produces a byte-identical request body to today — `test_schema_none_body_is_byte_identical_to_explicitly_passing_none`

### Part 5 — Feature 3: generation runtime (G0–G4) > Phases and checkpoints > Phase G1 — motif prose filter (no model, no framework) > ✅ CP-G1  _(plan)_
- `[x]` plan:741 — `一类`, `伤害`, `优先` gone from the three named regression demons — `bucketnutzombie` `['铁头功','僵尸']`, `cherrynut` `['僵尸','樱桃','喜爱']`, `cactus` `['仙人掌','发射']`; none carries a banned token
- `[x]` plan:742 — `为什么`/`是因为`/`不过` dropped by POS; `铁头功`/`坚果` kept — measured on the real narrative: all 5 connectives dropped, all 4 content words kept
- `[x]` plan:743 — `可在三种攻击模式之间切换` survives (CJK numeral is not an ASCII digit) — `classify_line` → `prose`; the sibling `伤害：20/1秒` → `mechanical`
- `[x]` plan:744 — Determinism and the ≤4-char token guarantee hold — 5 identical runs; 0 of 135 corpus motifs exceed 4 chars
- `[x]` plan:745 — Rise in `basis="name"`/`blocked` reported as a **result**, not a failure — 53 `text` / 31 `name`, unchanged by the `l`-tag fix; 0 non-blocked demons left motif-less

### Part 5 — Feature 3: generation runtime (G0–G4) > Phases and checkpoints > Phase G2 — workflow runtime (parallel with G1) > ✅ CP-G2  _(plan)_
- `[x]` plan:759 — **Zero** LangGraph imports in `nodes/`/`state.py`, asserted — grep over `seedsmith/workflow/` returns nothing outside `graphs/`; `test_nodes_and_state_never_import_langgraph`
- `[x]` plan:760 — Graph structure assertable **offline** — no model, no network — `test_graph_structure_is_inspectable_without_a_model_or_network`
- `[x]` plan:761 — Kill mid-run then resume; finished nodes do not re-call the model — `test_resume_replays_from_checkpoint_without_calling_the_model_again`
- `[x]` plan:762 — A deliberate routing bug is still stopped by `recursion_limit` — `test_recursion_limit_still_stops_a_deliberately_broken_router`
- `[x]` plan:763 — Transient vs quality retry are demonstrably different code paths — `llm_caller` attempts (transient) vs `route_after_validate` repair (quality); asserted separately

### Part 5 — Feature 3: generation runtime (G0–G4) > Phases and checkpoints > Phase G3 — quality gates > ✅ CP-G3  _(plan)_
- `[x]` plan:776 — All four validators exist with positive and negative tests — 6 validators, 18 tests in `test_quality_gates.py` (the 4 planned plus `language_consistency`, `subject_name_echo`)
- `[x]` plan:777 — Tier labelling exists; nothing reports a pass rate as quality — `test_a_passing_result_says_mechanically_valid_never_good`
- `[x]` plan:778 — CoVe present, **not wired into the default graph**, asserted — `test_cove_is_disabled_by_default`, `test_cove_is_not_wired_into_the_default_graph`
- `[x]` plan:779 — Zero real model calls in the suite — 480 tests pass with LM Studio unreachable from the scratch venv; every graph test injects `call=`

### Part 5 — Feature 3: generation runtime (G0–G4) > Phases and checkpoints > Phase G4 — commander-effect (the first real generator) > ✅ CP-G4 — closes the feature  _(plan)_
- `[x]` plan:791 — Every non-blocked demon has a commander effect; `Coverage/DemonUncovered` falls by that count — `seedsmith check ... --metric Coverage/DemonUncovered` → **no findings** (84 → 0)
- `[x]` plan:792 — `blocked` demons generate nothing, provably — `test_a_blocked_demon_is_not_a_subject`
- `[x]` plan:793 — An unprefixed id fails corpus load (the `wallnut` collision), asserted — `test_an_unprefixed_id_collides_with_the_demon_and_fails_corpus_load`
- `[x]` plan:794 — Re-run produces zero new writes — ⛔ **was unmet, now built.** Generator had no skip logic and rewrote all 84 stochastically. Added `_provenance.motifs` + `stale_ids()` + skip-existing; a plain re-run leaves `all.json` **byte-identical** (md5 `00b74afd…` before and after)
- `[x]` plan:795 — Quality reported from a read sample, **separately** from the pass rate — the 9 regenerated effects read individually; `normalzombie` 「随处可见的消耗」 → 「无名之辈」
- `[x]` plan:796 — Full suite green; four guard scripts green — 480 passed (both venvs); all four `guard-*.ps1` PASS

### Tasks: Seedsmith — full program > Phase 0 — `llm_caller` (independent — no dependency on Phase 1+) > S0 — port the LM Studio caller  _(todo)_
- `[x]` todo:46 — A stdlib `http.server` fixture captures the outgoing request body; test asserts both
- `[x]` todo:48 — `extract_json` fixtures: clean JSON, fenced JSON, prose-wrapped JSON, and one
- `[x]` todo:50 — `call_with_self_heal` fixture: a `verify_fn` that fails once then passes proves the retry
- `[x]` todo:52 — `call_with_self_heal` fixture: a `verify_fn` that never clears within `max_heal` falls back to
- `[x]` todo:54 — A test greps the module for `from seedsmith.(corpus|adapters|metrics)` and fails if found —
- `[x]` todo:56 — Suite runs fully offline — the mock server is the only "model" ever called
- `[x]` todo:57 — (beyond the original list, added on review) `load_config()` reads `[pipeline.llm_caller]`
- `[x]` todo:60 — (beyond the original list, added on review) `call_model` retries `attempts` times against a

### Tasks: Seedsmith — full program > Phase 1 — walking skeleton > S1 — corpus + stub adapter + one metric + report + CLI  _(todo)_
- `[x]` todo:100 — `seedsmith check --adapter stub` → clean fixture: `0`; broken fixture: `1`
- `[x]` todo:101 — Unreadable corpus → `2`, distinct from `1`, with a message naming the file
- `[x]` todo:102 — `Loop.OPEN` + `gates=True` raises at registration
- `[x]` todo:103 — A metric whose `needs` are unmet emits `NOT_MEASURED`, never a pass
- `[x]` todo:104 — `Finding` carries `schemaVersion`
- `[x]` todo:105 — Package is `seedsmith/__main__.py` — no `seedsmith.py` shadowing the package

### Tasks: Seedsmith — full program > Phase 2 — the real corpus > S2 — `adapter-items`  _(todo)_
- `[x]` todo:150 — **15** `KindSpec`s matching `KindCatalog.cs` — not 14: the catalog also carries `attribute`
- `[x]` todo:153 — `legal_combinations` encodes `frame=hybrid` excluding `ward-array`, `jewel-minor-b`, and the
- `[x]` todo:161 — `channels()` returns the 14 primary families (`bands.v1.json`'s `primaryChannel.memberFamilies`,
- `[x]` todo:167 — Registry versions reported, not assumed — measured fresh: naming/tags at v4, classes at v3,
- `[x]` todo:171 — `seedsmith check --adapter items` loads **1,430 entries across 121 files** — not 1,438/125.
- `[x]` todo:176 — `Coverage/EmptyPartition` reports **exactly nine**: `attributes`,
- `[x]` todo:181 — `attributes` is flagged as the deferred one (`KindSpec.required == {"id","nameKey","name"}`,

### Tasks: Seedsmith — full program > Phase 3 — parity and absorption > S3 — absorb `seed_graph`  _(todo)_
- `[x]` todo:232 — Port the seven check functions → Linkage + Registration families (**ten** finding codes, not
- `[x]` todo:242 — Port its 16 tests — all 16 pass unchanged in intent (`tests/test_linkage.py`)
- `[x]` todo:243 — `Acquisition` model: specific vs categorical grants both preserved
- `[x]` todo:245 — Parity harness: run both against the live corpus, diff finding sets
- `[x]` todo:249 — Finding sets **byte-identical** to `seed_graph` on the live corpus — `PARITY OK` on the
- `[x]` todo:252 — Parity harness is a test, not a one-off script — `ParityTests.test_finding_sets_are_...`
- `[x]` todo:259 — Categorical grants still resolve — verified live: 0 `Unobtainable` findings for `base-type`

### Tasks: Seedsmith — full program > Phase 4 — the measurement families > S4 — Coverage family  _(todo)_
- `[x]` todo:282 — `Coverage/EmptyPartition` (from S1), `Coverage/PairwiseHole`. **`Coverage/SlotUncovered`
- `[x]` todo:288 — Pairwise t-way over dimension pairs, illegal pairs excluded via `LegalityFn`
- `[x]` todo:289 — Fixture with a legal-but-missing pair **and** an illegal pair; only the first is a finding
- `[x]` todo:316 — **Wave-2's `(band, element)` uniques finding dropped from this task's claim** — `rarity`
- `[x]` todo:326 — Zero findings for illegal pairs — the false-positive flood that kills adoption
- `[x]` todo:329 — Reports `|seen|/|required|` per dimension pair (`evidence["seen"]`/`["required"]`)

### Tasks: Seedsmith — full program > Phase 4 — the measurement families > S5 — `numerics` + Balance family  _(todo)_
- `[x]` todo:343 — The four channel-group formulas, exactly as locked in `bands.v1.json`
- `[x]` todo:348 — `tier-bands.v1.json` created at `data/seed/items/_tuning/tier-bands.v1.json` (the spec's own
- `[x]` todo:351 — `ProgressionModel` protocol + `BattleRulesetProgression` — reads reference bases from
- `[x]` todo:354 — `resolve`, `explain`, `rebalance` (diff-then-publish, never mutates), `solve_base_share`
- `[x]` todo:357 — Guardrails: monotonicity, band containment, **`hi_t ≥ lo_(t+1)` — overlap REQUIRED, OD4**
- `[x]` todo:363 — `Balance/LadderInversion` via PAVA (`numerics/pava.py`); `Balance/OutOfEnvelope`
- `[x]` todo:364 — `content_ladder()` returning `None` → Balance reports `NOT_MEASURED`, never a pass —
- `[x]` todo:377 — Reproduces both committed examples: vitality 30‰×680→20, might 45‰×92→4
- `[x]` todo:379 — A channel with no authored share **raises**; it does not default (`UnsharedChannelError`)
- `[x]` todo:380 — Resolving at a hardcoded calibration level (20) raises unless explicitly requested
- `[x]` todo:382 — Resolves against the **stub** adapter with no `bands.v1.json` present (proves B2 is fixed) —

### Tasks: Seedsmith — full program > Phase 4 — the measurement families > S6 — `budget` + Distribution family  _(todo)_
- `[x]` todo:408 — `budget derive` emits targets with **provenance and conflicts preserved** — scoped to three
- `[x]` todo:416 — Conflicted rows block distribution checks and report the conflict instead
- `[x]` todo:418 — Structural / stated / proportional derivation, in that preference order (the uniques row
- `[x]` todo:421 — Largest-remainder for proportional splits (`numerics.apportion`, built in S5, reused here —
- `[x]` todo:424 — `Distribution/CellDeviation`, `/Evenness` (Pielou **and** richness), `/Inequality` (Gini)
- `[x]` todo:425 — All Distribution metrics `gates=False` for W1
- `[x]` todo:428 — The uniques row shows all three conflicting counts (20 / 300 / 144) with sources
- `[x]` todo:432 — Proportional rows carry `derivation=PROPORTIONAL` and wider tolerance (±15%, vs 0 for the
- `[x]` todo:434 — **Wave-2's "humanoid uniques half as common as plant" dropped from this task's claim** —
- `[x]` todo:447 — Degenerate cases handled: `S=0`→skip silently when observed is also 0, `S=1`→Pielou defined

### Tasks: Seedsmith — full program > Phase 4 — the measurement families > S7 — Constraint · ExemplarConformance · SemanticDedup  _(todo)_
- `[x]` todo:463 — `Constraint`: a manifest of rule → check bindings (`metrics/constraint.py`); a documented
- `[x]` todo:471 — `ExemplarConformance`: every exemplar validates as real content of its kind
- `[x]` todo:474 — `SemanticDedup`: exact + canonical + MinHash/LSH near-duplicates (`metrics/dedup.py`) — one
- `[x]` todo:480 — Conceptual clustering listed as a **known gap**, blocked on the adjective `axis` addition
- `[x]` todo:484 — Catches the three historical exemplar defects when replayed as fixtures: missing
- `[x]` todo:490 — Would have caught `gem.g1-015` / `consumable.k1-007` both named "Mending Pulse" — replayed
- `[x]` todo:494 — `seedsmith metrics --coverage` prints the unclaimed Appendix-A row rather than hiding it —

### Tasks: Seedsmith — full program > Phase 4 — the measurement families > S8 — sampling + Quality  _(todo)_
- `[x]` todo:516 — Stratified sampling (`seedsmith/sampling/`), seeded from `metric id + corpus revision`
- `[x]` todo:521 — `Quality/FlavourMissing` (closed), `Quality/FlavourGeneric` (open) — scoped to six
- `[x]` todo:528 — Open-loop metrics emit a review queue and **never** a pass — `FlavourGeneric` only ever
- `[x]` todo:545 — Same seed → same sample, **across separate OS processes**, not just repeated in-process
- `[x]` todo:548 — Stratification covers every band, not just the largest
- `[x]` todo:551 — Reproduces *"60 consumables have no flavour, 30 of 70 charms"* — **exactly**, verified

### Tasks: Seedsmith — full program > Phase 5 — trust and cutover > S9 — mutation testing over the metric suite  _(todo)_
- `[x]` todo:575 — Mutants at `scripts/mutants/seedsmith.json` (single file, matching the existing convention's
- `[x]` todo:578 — Invert a comparison, drop a guard, widen a regex — one per metric family: Coverage
- `[x]` todo:587 — `scripts/mutate.ps1` **extended, not replaced** — the shared C# mutation runner other active
- `[x]` todo:611 — Every metric family has ≥1 mutant its fixtures kill — **10/10 mutants caught**, verified live
- `[x]` todo:613 — Deliberately re-introducing the inverted `hi_t < lo_(t+1)` guardrail is **killed** —
- `[x]` todo:618 — `.\scripts\mutate.ps1 -Set seedsmith` runs, exits `0` on a clean suite and `1` on a survivor

### Tasks: Seedsmith — full program > Phase 5 — trust and cutover > S10 — CI cutover  _(todo)_
- `[x]` todo:626 — Step 1: parity diff green on the live corpus — done in S3, re-confirmed as part of wiring
- `[x]` todo:628 — Step 2 — **superseded by stronger evidence than the plan asked for, not skipped.** The
- `[x]` todo:648 — Step 3: CI switches to `seedsmith check --gate`; `seed_graph` steps removed. **One thing the
- `[x]` todo:668 — Step 4: `tools/seed_graph/` deleted (9 files: `corpus.py`, `checks.py`, `__init__.py`,
- `[x]` todo:674 — `tools/ItemSeedValidator` untouched throughout — it stays the referential gate (confirmed
- `[x]` todo:678 — CI green at every step; no step leaves the build red — the exact new step commands were run
- `[x]` todo:682 — Suite runs inside the 30 s budget — measured live: **4.87s–5.75s** wall-clock across several
- `[x]` todo:684 — `tools/seed_graph/` gone, its 16 tests alive inside seedsmith — confirmed: `ls tools/`

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 1 — Feasibility and ordering > P1 — Feasibility: pigeonhole → Hopcroft–Karp → König ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:731 — Layer 1: pigeonhole sum check (Σdemand > Σcapacity ⇒ infeasible), O(n) — short-circuits before
- `[x]` todo:733 — Layer 2: max bipartite matching (demand ↔ slot graph) via Hopcroft–Karp, O(E√V) — chosen over
- `[x]` todo:736 — Layer 3: on infeasible, König's theorem names the binding constraint (min vertex cover), not
- `[x]` todo:738 — Balanced-case construction: cyclic Latin square `axis = (roleIndex + themeIndex) mod n`,
- `[x]` todo:740 — Slot capacity > 1 expanded into seats generated in sorted order, so an assignment is reproducible
- `[x]` todo:743 — A synthetic 5-themes×15-uniques-into-8-roles×5-axes fixture (mirrors the real 75-into-40
- `[x]` todo:746 — A balanced 5-theme fixture's Latin square produces 0 collisions across all 25 (role, theme)
- `[x]` todo:749 — A feasible-but-locally-starved fixture is caught by layer 2 where layer 1 would pass it —

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 1 — Feasibility and ordering > P2 — Ordering: derive kind-level stages, never hand-label them ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:768 — Prerequisite: extend `KindSpec` with `reference_fields: frozenset[str]` per kind — done first,
- `[x]` todo:773 — Build the kind-level reference graph via `corpus.discover_edges` (S1, reused) — entry-level
- `[x]` todo:776 — Kahn's topological sort into layers
- `[x]` todo:777 — Tarjan's SCC names a cycle's exact members, never just "cycle detected"
- `[x]` todo:780 — Reproduces the real historical order on the real corpus — `drop-table` lands after
- `[x]` todo:783 — A synthetic two-kind cycle fixture is caught and both kinds are named by Tarjan's SCC
- `[x]` todo:784 — No hand-maintained stage label remains anywhere in the adapter

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 2 — Validation, scheduling, and the demand split > P3 — Input validation: exemplar gate before dispatch ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:803 — Every exemplar a work order references is checked via `ExemplarConformance` (S7, reused, not
- `[x]` todo:807 — A failing exemplar refuses the whole order (`EXIT_EXEMPLAR_REFUSED = 3`, a named constant per
- `[x]` todo:809 — Placed **before dispatch, not after generation** — a bad exemplar caught afterwards has
- `[x]` todo:813 — Scoping (`referenced_kinds`): a gate that refuses an order over a kind the order never
- `[x]` todo:818 — A synthetic exemplar with a missing required field refuses the order, not a partial emit —
- `[x]` todo:821 — A clean exemplar set passes through untouched — plus an empty corpus, which must pass rather

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 2 — Validation, scheduling, and the demand split > P4 — Scheduling and work-order output ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:835 — List scheduling: layer-by-layer (P2), longest-job-first within a layer
- `[x]` todo:836 — Model tier by a small adjustable rule table, not an optimizer — a test swaps the table and
- `[x]` todo:838 — Emits the JSON work order per spec-planner.md §6's shape, `closes` naming every `Finding`
- `[x]` todo:840 — An excluded partition is **reported, never silently dropped** (`excluded[]` +
- `[x]` todo:843 — ⛔ **Spec conflict found and resolved with evidence, not picked:** `spec-planner.md` §7 said
- `[x]` todo:852 — `gems/2` placed after its registry dependency; the three `display-templates/{4,5,6}`
- `[x]` todo:856 — The four S2-mislabeled base-type partitions are correctly EXCLUDED as generation jobs

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 2 — Validation, scheduling, and the demand split > P5 — Generation pipelines: the declare/fulfil split ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:869 — Phase A (declare): deterministic per-kind stages emit `Demand` objects — no generation, no
- `[x]` todo:872 — Phase B (fulfil): topological sort of the full demand graph, feasibility check (P1), resolve
- `[x]` todo:880 — A synthetic 3-set-theme fixture with overlapping demand reuses existing base types first and
- `[x]` todo:885 — A recipe fixture proves materials are demanded (and generated) before the recipe consuming

### Tasks: Seedsmith — full program > Part 2 — W2: planning (`planner` + `briefkit`) > Phase 3 — `briefkit` > P6 — Work order → briefs ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:910 — Assembles from: allocation (planner), budget row (target/tolerance/rationale), adapter
- `[x]` todo:916 — Content-addressed: brief hash is a pure function of its inputs, recorded in the job
- `[x]` todo:917 — An empty vocabulary says so rather than being omitted — an absent section reads as "no
- `[x]` todo:921 — A brief for `gems/2` inlines the literal legal `family` vocabulary — grepped and a planted
- `[x]` todo:923 — Two brief generations from byte-identical inputs produce the identical content hash — plus the
- `[x]` todo:925 — A brief whose exemplar failed P3's gate is never emitted — checked once for the whole batch,

### Tasks: Seedsmith — full program > Part 3 — W3: generation (`pipeline`) > Phase 4 — `pipeline` generation logic > G1 — Pipeline scaffold: schema-per-metric + guardrails ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:956 — `Pipeline` dataclass (metric, scope, schema, gate, max_retries, on_persist, model) per
- `[x]` todo:958 — JSON Schema validated locally always; via the model's structured-output mode where supported
- `[x]` todo:959 — Narrow scope per call; closed vocabularies inlined (reuses `briefkit`, not reimplemented)
- `[x]` todo:960 — **Never a number** — `audit_schema` walks `properties`, `items` and the composition keywords,
- `[x]` todo:965 — Validate-before-accept: scratch → gate → move
- `[x]` todo:966 — Bounded retry with the exact error attached, then escalate — `llm_caller.call_with_self_heal`
- `[x]` todo:969 — Every schema carries a `blocked` variant with a reason string — `blocked` is never retried
- `[x]` todo:974 — Schema-audit test rejects any pipeline whose schema has a bare numeric field — plus a positive
- `[x]` todo:977 — A fixture pipeline against a fake model server proves retry-with-named-defect then
- `[x]` todo:981 — A `blocked` response writes nothing and is reported, not treated as a failure

### Tasks: Seedsmith — full program > Part 3 — W3: generation (`pipeline`) > Phase 4 — `pipeline` generation logic > G2 — Idempotence and provenance ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:999 — Every generated entry records `_provenance` (pipeline id, model, prompt version, budget
- `[x]` todo:1002 — Re-running checks the finding is already closed (via a `metrics` re-run) **before** generating
- `[x]` todo:1005 — Re-recording a row **raises** rather than last-write-wins — two runs both believing they
- `[x]` todo:1009 — Running a pipeline twice over unchanged input produces zero new writes the second time — the
- `[x]` todo:1011 — Provenance is queryable by finding id — both halves: why does this row exist (the finding),

### Tasks: Seedsmith — full program > Part 3 — W3: generation (`pipeline`) > Phase 4 — `pipeline` generation logic > G3 — Open-loop review queue wiring ✅ BUILT 2026-08-31  _(todo)_
- `[x]` todo:1030 — Wires an open-loop pipeline to `seedsmith/sampling/` (S8, reused — asserted structurally via
- `[x]` todo:1033 — `audit_open_loop_schema` rejects any verdict field — `pass`/`ok`/`valid`/`quality`/`score`/
- `[x]` todo:1036 — `blocked` is explicitly **not** a verdict — declining is not a judgement about quality;
- `[x]` todo:1038 — Over-refusal guarded too — `passage` must not trip the `pass` rule
- `[x]` todo:1041 — An open-loop pipeline's schema never includes a pass/fail field
- `[x]` todo:1042 — Re-running `metrics` after generation still reports the finding as open-loop, never a silent

### Part 4 — Feature 2: demons (D1–D4) > Phase D1 — foundation, zero model calls  _(todo)_
- `[x]` todo:1119 — **D1.1 — `DemonCorpusBuilder`, pure** · **M** · **Deps:** none ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1121 — Pure `(species, almanacRows, recipeRows) -> entries`; no filesystem, no DAL, no clock
- `[x]` todo:1122 — A type with no `spawn_stats` sample ⇒ `hp`/`attack`/`armor` **null** and
- `[x]` todo:1124 — `cost_status = 'unparsed'` stays distinct from `'absent'` in `coverage` —
- `[x]` todo:1126 — A catalog species with no `almanac_seed` row ⇒ entry still emitted, captured fields null —
- `[x]` todo:1128 — `lineage` populated from `recipes`; **`families` absent from every entry** (§2.4) —
- `[x]` todo:1131 — Catalog fields (element, rarity) **absent** — `Catalog_only_fields_never_appear_on_the_emitted_shape`
- `[x]` todo:1132 — `hp`/`attack`/`armor` are `long` end to end — `AlmanacSeedRow`/`DemonCorpusEntry` fields typed `long?`
- `[x]` todo:1151 — **D1.2 — `tools/DemonCorpusEmit` + committed corpus** · **M** · **Deps:** D1.1 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1153 — `dotnet run --project tools/DemonCorpusEmit -- <data dir>` writes `data/seed/demons/*.json` —
- `[x]` todo:1156 — Calls `DerivedStatPolicy.Configure` **before** constructing `RpgStore`
- `[x]` todo:1157 — Run twice ⇒ **byte-identical** files — ran to two independent output roots,
- `[x]` todo:1159 — Output loads through seedsmith's `Corpus.load` with **no adapter changes** — loaded live:
- `[x]` todo:1162 — All SQL stays inside `FusionRpg.Data` — `.\scripts\guard-dal.ps1` → **DAL GUARD OK**
- `[x]` todo:1172 — **D1.3 — `DemonsAdapter`, the five methods** · **M** · **Deps:** D1.2 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1174 — `channels()` returns **empty** — `test_channels_are_empty` (A4)
- `[x]` todo:1175 — `kinds()` contains **no `item` and no `action`** — `test_item_and_action_kinds_are_absent` (A3)
- `[x]` todo:1176 — `legal_combinations()`'s **`False` branch is reachable and exercised** —
- `[x]` todo:1181 — `family` dimension declared with **empty values**; partitioning falls back to `side/rarity` —
- `[x]` todo:1183 — `environment` declared but its partitions **excluded from coverage**, with the reason in a
- `[x]` todo:1185 — Every registry vocabulary non-empty and **inlinable** — checked against `briefkit.render`'s
- `[x]` todo:1187 — Motif expression rules present for **every** kind (including `demon` itself, a deliberate
- `[x]` todo:1189 — `reference_fields` declares `demonId` on `aspect`/`commander-effect`/`environment`
- `[x]` todo:1190 — **`test_stub_adapter.py` still passes** — confirmed twice: once by pytest's own collection,
- `[x]` todo:1192 — ⛔ **§D-F1 applied, not just planned:** `adapters/base.py` gained one additive
- `[x]` todo:1216 — **D1.4 — D1 integration** · **S** · **Deps:** D1.3 ✅ **VERIFIED 2026-08-31 against the live corpus**
- `[x]` todo:1218 — The emitted corpus loads and **every existing metric runs against it** — zero model calls.
- `[x]` todo:1225 — Coverage reports real partitions; `environment` is absent from them — trivially and

### Part 4 — Feature 2: demons (D1–D4) > Phase D1 — foundation, zero model calls > ✅ CP-D1 — **REACHED 2026-08-31**  _(todo)_
- `[x]` todo:1233 — Full seedsmith suite green **including `test_stub_adapter.py`** — 315/315, `test_stub_adapter.py`
- `[x]` todo:1235 — `dotnet test tests\FusionRpg.Core.Tests` green — **4896/4896** (was 4887; +9 from D1.1's
- `[x]` todo:1239 — Emitter byte-identical across two runs — `diff -rq` on two independent output roots, **zero
- `[x]` todo:1241 — §D-F1 finding written into `spec-adapter-demons` §1/§4 — both sections corrected in place,
- `[x]` todo:1243 — **D1 is shippable alone** — demons queryable by every existing metric, no model calls. Proven

### Part 4 — Feature 2: demons (D1–D4) > Phase D2 — taxonomy (first model calls, all faked)  _(todo)_
- `[x]` todo:1249 — **D2.1 — `family-extract`** · **M** · **Deps:** D1.4 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1251 — Batching over the same corpus repeatedly ⇒ **identical batches, byte for byte** —
- `[x]` todo:1253 — Batch size is a **structural constant (8)** with a comment saying what it trades; not a tunable
- `[x]` todo:1254 — Each candidate carries `label` (English), `nativeLabel` (as read), `basis`
- `[x]` todo:1255 — `basis` ∈ {`text`, `name`, `blocked`}; a `blocked` demon carries **no label at all** and it
- `[x]` todo:1258 — Three siblings in one batch **can receive one shared label** —
- `[x]` todo:1262 — **Falsifier:** the same fixture at batch size 1 produces three *distinct* labels — proving
- `[x]` todo:1264 — Two candidates differing only in `nativeLabel` still merge downstream — extraction's own
- `[x]` todo:1268 — A demon may receive **more than one** label from one batch —
- `[x]` todo:1270 — Schema passes `audit_schema` (**no numeric field**) and `audit_open_loop_schema` (**no
- `[x]` todo:1272 — A label returned for a demon **not in the batch** is rejected, not recorded —
- `[x]` todo:1277 — Brief contains no citation-shaped string — `test_brief_contains_no_citation_shaped_text`,
- `[x]` todo:1280 — **Zero real model calls** — `MockModelServer` imported from `test_llm_caller`, not
- `[x]` todo:1291 — **D2.2 — `family-consolidate`** · **M** · **Deps:** D2.1 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1293 — Same candidates consolidated twice ⇒ **byte-identical** vocabulary and assignments —
- `[x]` todo:1295 — `wall-nut`, `defensive-nut`, `nut-type` ⇒ one family, head noun `nut` — matches the
- `[x]` todo:1298 — `shell` + `armor-plated` in the synonym map ⇒ merged
- `[x]` todo:1299 — **Empty synonym map ⇒ NOT merged** — `test_shell_and_armor_plated_do_not_merge_with_an_empty_synonym_map`
- `[x]` todo:1300 — `basis = "blocked"` ⇒ **zero** families, not an error — structural, not a special case: a
- `[x]` todo:1302 — Candidates from two heads ⇒ **both** families (multi-membership)
- `[x]` todo:1303 — Adding a demon and re-running: existing ids unchanged, new family **appended at the end**
- `[x]` todo:1304 — A family with no supporting candidate is **rejected** — consolidation cannot invent (§2.5).
- `[x]` todo:1310 — The merged family carries its contributing `nativeLabel`s
- `[x]` todo:1320 — **D2.3 — `motif-derive`** · **M** · **Deps:** D2.2 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1322 — Same corpus derived twice ⇒ **byte-identical** assignments — `test_same_corpus_derived_twice_is_byte_identical`
- `[x]` todo:1323 — Demon in two families **inherits from both** — `test_demon_in_two_families_inherits_from_both`
- `[x]` todo:1324 — Demon in no family, with text ⇒ motifs from own text, `basis = "text"`
- `[x]` todo:1325 — Demon in no family, no text ⇒ **no motifs**, `basis = "blocked"` — not padded, not an error
- `[x]` todo:1326 — Family with `basis = "name"` ⇒ inherited motifs carry `basis = "name"`, not `"text"` —
- `[x]` todo:1330 — Motif count **≤5** wherever any motif exists; ordering **family-first** and stable
- `[x]` todo:1331 — Anti-motifs drawn from the contrasting family; non-empty **wherever another family exists
- `[x]` todo:1334 — `DerivedMotifs` contains **no numeric field** — `test_derived_motifs_carries_no_numeric_field`
- `[x]` todo:1336 — ⛔ **A2's tautology case is FLAGGED in the output** — `test_a2_tautology_flagged_when_own_and_every_family_are_basis_name`,

### Part 4 — Feature 2: demons (D1–D4) > Phase D2 — taxonomy (first model calls, all faked) > ✅ CP-D2 — **REACHED 2026-08-31**  _(todo)_
- `[x]` todo:1370 — Every D2 artifact byte-identical across re-runs — proven per-module (D2.1/D2.2/D2.3 each have
- `[x]` todo:1373 — **Zero real model calls anywhere in the suite** — `test_zero_real_model_calls_every_request_hits_loopback`
- `[x]` todo:1375 — `blocked` propagates end to end and is never an error — traced through all three modules:
- `[x]` todo:1379 — Append-only proven: adding a demon leaves existing family and motif ids untouched —
- `[x]` todo:1382 — `python -m pytest -q` full suite green — **362/362**

### Part 4 — Feature 2: demons (D1–D4) > Phase D3 — measurement (gates D4)  _(todo)_
- `[x]` todo:1386 — **D3.1 — `Coverage/DemonUncovered`** · **S** · **Deps:** D2.3 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1388 — Every demon has content ⇒ reports **nothing** — `test_every_demon_has_content_reports_nothing`
- `[x]` todo:1389 — ⛔ **A5's exact case:** one demon uncovered while **all its families are covered** ⇒ **one
- `[x]` todo:1391 — "Content" = **any** generated artifact; the finding's evidence carries a **per-kind
- `[x]` todo:1397 — A demon with a commander effect but no aspect ⇒ **covered**, no finding —
- `[x]` todo:1399 — `gates = False`; `loop = CLOSED`; deterministic finding order — `test_finding_ordering_is_stable_across_runs`
- `[x]` todo:1400 — **Fully generic** (spec §4's own requirement: "work for a non-demon adapter that supplies
- `[x]` todo:1415 — **D3.2 — `Distribution/MotifSharing`** · **S** · **Deps:** D2.3 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1417 — Reports `demonsPerMotif`, **`demonCount`**, `excludedTautological`, `singleUseMotifs` —
- `[x]` todo:1422 — A demon with motifs **and** families both `basis = "name"` is **excluded** from numerator
- `[x]` todo:1426 — ⛔ **The decisive test:** a **wholly tautological corpus reports "cannot be measured"**, not
- `[x]` todo:1428 — Motifs each used once ⇒ `singleUseMotifs` equals vocabulary size; sharing reported absent —
- `[x]` todo:1430 — `loop = OPEN`; schema carries **no pass/fail field** — `test_schema_carries_no_pass_fail_field`;
- `[x]` todo:1433 — `singleUseMotifs` reports **all** — no suppression threshold anywhere in the implementation
- `[x]` todo:1434 — `demonCount` reported in **every** branch (no-motif-data, all-tautological, and the normal

### Part 4 — Feature 2: demons (D1–D4) > Phase D3 — measurement (gates D4) > ✅ CP-D3 — THE GATE — **REACHED 2026-08-31**  _(todo)_
- `[x]` todo:1450 — Both metrics ship `gates = False` — `test_both_metrics_ship_non_gating`
- `[x]` todo:1451 — Both live in `metrics/` and work for a **non-demon** adapter supplying the same strata —
- `[x]` todo:1454 — **The tautology test passes — D4 does not start until it does.**

### Part 4 — Feature 2: demons (D1–D4) > Phase D4 — consumption  _(todo)_
- `[x]` todo:1460 — **D4.1 — theme registry + items vocabulary** · **M** · **Deps:** CP-D3 ✅ **BUILT + VERIFIED 2026-08-31**
- `[x]` todo:1462 — Emits `data/seed/demons/_registry/themes.v1.json`, append-only, sorted keys — **REAL and
- `[x]` todo:1467 — Every demon theme id is **`demon.*`**-prefixed; collision with legacy `theme.*` is
- `[x]` todo:1471 — A theme carries motifs, anti-motifs, **expression rules**, `basis`, and the **`rarity` it
- `[x]` todo:1474 — A demon with `basis = "blocked"` **publishes no theme** — `test_a_blocked_demon_publishes_no_theme`
- `[x]` todo:1475 — A demon with `basis = "name"` publishes a theme **marked as such** —
- `[x]` todo:1477 — A theme without expression rules **fails validation** — structurally guaranteed (no
- `[x]` todo:1480 — Items' `themeKey` becomes a registry-backed vocabulary; a key in **neither** population is
- `[x]` todo:1497 — **D4.2 — coexistence and churn proof** · **S** · **Deps:** D4.1 ✅ **VERIFIED 2026-08-31 against the live corpus**
- `[x]` todo:1499 — ⛔ **All 38 existing themed entries still validate** (corrected count — see D4.1's own
- `[x]` todo:1504 — A legacy `theme.*` key validates alongside `demon.*` keys —
- `[x]` todo:1506 — A demon that leaves the roster ⇒ its theme is **retired (`retired: true`), still
- `[x]` todo:1509 — A re-run with a new demon leaves existing keys untouched — same mechanism D2.2 already
- `[x]` todo:1511 — **Direction asserted structurally:** nothing in `adapters/demons/` reads the items corpus —

### Part 4 — Feature 2: demons (D1–D4) > Phase D4 — consumption > ✅ CP-D4 — closes Part 4 — **REACHED 2026-08-31**  _(todo)_
- `[x]` todo:1520 — Full seedsmith suite green — **392/392**
- `[x]` todo:1521 — `dotnet test` green across Core / Data / Guard — see the full-program sweep below;
- `[x]` todo:1523 — Exactly **one** file outside `adapters/demons/` changed in D4 — `adapters/items/registries.py`,
- `[x]` todo:1525 — An item can be authored themed to a demon and validates —

### Part 4 — Feature 2: demons (D1–D4) > The real generation run — owner-authorized 2026-09-01, after CP-D4  _(todo)_
- `[x]` todo:1546 — **`family-candidates.json`** — 11 real batches, 84 demons, 104.3s wall-clock. **53/84 demons
- `[x]` todo:1557 — **`families.v1.json` + `family-assignments.json`** — 53 candidates → **19 real families**
- `[x]` todo:1572 — **`motifs.v1.json` + `motif-assignments.json`** — **84/84 demons** motif-derived (0 blocked,
- `[x]` todo:1591 — **`themes.v1.json`** — **84 real themes published**, all `demon.*`-prefixed, 0 blocked.
- `[x]` todo:1594 — **All 38 pre-existing legacy `theme.*` themed entries re-verified against the real corpus

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G0 — dependency baseline ⛔ BLOCKING  _(todo)_
- `[x]` todo:1624 — **G0.1 — `pyproject.toml`, exact pins, lockfile, isolated venv** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1626 — `pyproject.toml` declares `jieba`, `langgraph==1.2.11`, `langgraph-checkpoint-sqlite`
- `[x]` todo:1627 — Every pin is `==`, **never** `>=` — LangGraph shipped 10 releases in 2026-04 alone
- `[x]` todo:1628 — `requirements.lock` committed with the full transitive set — **44 packages** (corrected from the planned '~31'), incl. `langsmith`
- `[x]` todo:1629 — CI installs from the lockfile in a clean environment
- `[x]` todo:1630 — ⚠️ CI step positioned so its failure **cannot be masked** by the known `ci.yml` defect
- `[x]` todo:1635 — **G0.2 — offline guarantee as a test** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1637 — `LANGSMITH_TRACING` and `LANGCHAIN_TRACING_V2` asserted **unset**
- `[x]` todo:1638 — A graph runs under a socket guard raising on any non-loopback connect → **zero attempts**
- `[x]` todo:1639 — Test uses stdlib `socket` patching — no new dependency to test that we have few
- `[x]` todo:1643 — **G0.3 — `response_format` constrained decoding in `llm_caller`** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1645 — Optional `schema` parameter, `None` default
- `[x]` todo:1646 — ⛔ `call_model(schema=None)` produces a **byte-identical** request body to today — this
- `[x]` todo:1648 — With a schema: response parses with plain `json.loads`, no fence stripping needed
- `[x]` todo:1649 — An `enum` field cannot produce an out-of-enum value (measured: it could not)
- `[x]` todo:1650 — `extract_json` **still present and still tested** — defense-in-depth, not replaced

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G0 — dependency baseline ⛔ BLOCKING > ✅ CP-G0 — **REACHED 2026-09-01**  _(todo)_
- `[x]` todo:1655 — Fresh clone + clean venv + lockfile install + full suite **passes** — created `.venv-verify`,
- `[x]` todo:1657 — `import jieba` succeeds in a fresh venv — **jieba 0.42.1 OK**; the D2.3 debt is paid
- `[x]` todo:1658 — Offline guarantee is a passing test — **4/4**, including a falsifier proving the socket guard
- `[x]` todo:1660 — Every existing `llm_caller` caller is provably unchanged —

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G1 — motif prose filter (no model, no framework)  _(todo)_
- `[x]` todo:1678 — **G1.1 — four-rule line classifier** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1680 — `韧性：270+2200（一类）`, `伤害：20/1.5秒` → **mechanical** (rule 1)
- `[x]` todo:1681 — `特点：…`, `融合配方：…` → **mechanical** (rule 2)
- `[x]` todo:1682 — ⛔ `②处于火力覆盖模式时…` → **mechanical** (rule 3) — the 13-line leak the audit found
- `[x]` todo:1683 — ⛔ `对于血量高于50%的…` → **mechanical** (rule 4)
- `[x]` todo:1684 — ⛔ `可在三种攻击模式之间切换` → **prose** — proves rule 4 is ASCII-only and does not over-filter
- `[x]` todo:1685 — A prose sentence with a mid-clause colon → **prose** (the ≤12-char label bound)
- `[x]` todo:1686 — `classify_line` is a pure function, exported and tested directly
- `[x]` todo:1690 — **G1.2 — POS filtering via `jieba.posseg`** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1692 — Keep `n*`/`v*`/`a*`/`i`/`l`; drop `r`/`c`/`d`/`p`/`u`/`t`
- `[x]` todo:1693 — ⛔ `为什么`/r, `是因为`/c, `不过`/c **dropped** — the `bucketnutzombie` regression, pinned
- `[x]` todo:1694 — ⛔ `铁头功`/n, `坚果`/n, `练成`/v **kept** — proves it is not deleting everything
- `[x]` todo:1695 — `_CJK_STOPWORDS` reduced to a small override list, no longer the primary mechanism
- `[x]` todo:1698 — **G1.3 — wire in, regenerate, verify** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1700 — `flavorIntroduce` preferred where present (18/84 demons)
- `[x]` todo:1701 — ⛔ Three named regression demons: `一类`, `伤害`, `优先` **gone**
- `[x]` todo:1702 — Same corpus filtered twice → byte-identical
- `[x]` todo:1703 — Max token length still ≤4 (the D2.3 whole-clause guard)
- `[x]` todo:1704 — A demon losing all prose falls back to name (`basis="name"`) — **not an error**
- `[x]` todo:1705 — Rise in `basis="name"`/`blocked` counts **reported as a result**

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G1 — motif prose filter (no model, no framework) > ✅ CP-G1 — **REACHED 2026-09-01**  _(todo)_
- `[x]` todo:1713 — Stat lines contribute **zero** tokens — all three named regression demons fixed:
- `[x]` todo:1719 — Both POS regression sets pinned — `test_pos_filter_drops_narrative_connectives_from_flavor_introduce`
- `[x]` todo:1723 — Determinism and ≤4-char guarantee hold — regenerated twice, `diff` **byte-identical**;
- `[x]` todo:1725 — `motifs.v1.json` regenerated as a reviewed act, before any content binds to it — 84 demons,

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G2 — workflow runtime (parallel with G1 once G0 lands)  _(todo)_
- `[x]` todo:1746 — **G2.1 — state + nodes, no LangGraph** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1748 — `GenerationState` TypedDict; every field bounded, **no `messages` accumulator**
- `[x]` todo:1749 — `nodes/` are plain `(state) -> dict` functions, unit-testable with a plain dict
- `[x]` todo:1750 — ⛔ **Seam test: zero LangGraph imports in `nodes/` or `state.py`** — asserted by grep,
- `[x]` todo:1755 — **G2.2 — graph skeleton and bounded loops** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1757 — `START → brief → generate → validate → route → {persist|generate|escalate} → END`
- `[x]` todo:1758 — Three independent stops: `attempts`, `recursion_limit`, terminal `escalate`
- `[x]` todo:1759 — ⛔ A deliberate routing bug is **still stopped** by `recursion_limit` — the backstop is
- `[x]` todo:1761 — Clean draft → `attempts == 1`; defective → repair carries the **named** defect
- `[x]` todo:1762 — Never-clearing draft → **escalates**, writes nothing
- `[x]` todo:1763 — **No unbounded `while`** anywhere in the module
- `[x]` todo:1766 — **G2.3 — checkpointing and resume** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1768 — `SqliteSaver`, thread-id per subject
- `[x]` todo:1769 — ⛔ Kill mid-run, re-invoke same thread-id → resumes; **finished nodes do not re-call the model**
- `[x]` todo:1770 — ⛔ **Transient** retry = replay from checkpoint, **zero** new model calls
- `[x]` todo:1771 — ⛔ **Quality** retry = a genuinely new generation with the defect attached
- `[x]` todo:1772 — The two are demonstrably **different code paths**
- `[x]` todo:1773 — `sqlite3` used for checkpoints only; Python still never reads the game's SQLite
- `[x]` todo:1776 — **G2.4 — bounded fan-out runner** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1778 — Bounded worker count, a structural constant with a comment (not a tunable)
- `[x]` todo:1779 — Results deterministic per subject regardless of completion order

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G2 — workflow runtime (parallel with G1 once G0 lands) > ✅ CP-G2 — **REACHED 2026-09-01**  _(todo)_
- `[x]` todo:1783 — Zero LangGraph imports outside `graphs/`, asserted —
- `[x]` todo:1786 — Graph structure assertable **offline** — nodes enumerable and mermaid emitted with no model
- `[x]` todo:1788 — Crash-resume works and skips completed nodes —
- `[x]` todo:1791 — `recursion_limit` backstop exercised — `test_recursion_limit_still_stops_a_deliberately_broken_router`
- `[x]` todo:1794 — Transient vs quality retry proven distinct — transient is `resume()` (replay, **zero** new

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G3 — quality gates  _(todo)_
- `[x]` todo:1810 — **G3.1 — deterministic validator library** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1812 — `motif_coverage` rejects output using none of the subject's motifs
- `[x]` todo:1813 — `anti_motif_violation` rejects output using a word the subject is defined against
- `[x]` todo:1814 — ⛔ `field_echo` **rejects** `{"doctrine": "DOCTRINE: …"}` — the exact observed defect
- `[x]` todo:1816 — ⛔ `field_echo` **accepts** `{"doctrine": "The doctrine of …"}` — over-refusal is its own
- `[x]` todo:1818 — `non_empty` rejects empty/whitespace required fields
- `[x]` todo:1819 — Defect strings name the **field and the offending value** (they feed the repair prompt)
- `[x]` todo:1822 — **G3.2 — tier labelling** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1824 — Every validator result carries its tier
- `[x]` todo:1825 — ⛔ No summary anywhere reports a tier-2 pass rate as "quality" — the measured 8/8-on-bad-
- `[x]` todo:1829 — **G3.3 — CoVe: specified, wired off** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1831 — ⛔ Every verification question **answerable from source alone** — a subjective question is
- `[x]` todo:1833 — Verifier is **not shown the draft's justification**, asserted structurally
- `[x]` todo:1834 — Rejects only on **explicit contradiction**; rejection → **escalate**, never auto-repair
- `[x]` todo:1835 — CoVe schema carries **no verdict field**
- `[x]` todo:1836 — ⛔ **Not wired into the default graph**, asserted — specified, not built
- `[x]` todo:1837 — Self-consistency implemented and **asserted off**

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G3 — quality gates > ✅ CP-G3 — **REACHED 2026-09-01**  _(todo)_
- `[x]` todo:1841 — Four validators with positive **and** negative tests — 15/15 in `test_quality_gates.py`.
- `[x]` todo:1846 — Tier labelling enforced — `ValidatorResult.summary()` renders
- `[x]` todo:1849 — CoVe present, disabled, asserted — `COVE_ENABLED is False`;
- `[x]` todo:1852 — Zero real model calls in the suite — every CoVe/validator test uses injected callables

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G4 — commander-effect (the first real generator)  _(todo)_
- `[x]` todo:1869 — **G4.1 — brief, schema, gate** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1871 — Brief inlines motifs, anti-motifs and the expression rule **literally**; cites nothing
- `[x]` todo:1872 — Schema passes `audit_schema` (**no numeric field**) and `audit_open_loop_schema`
- `[x]` todo:1873 — Schema audited at **import time** (an unusable schema cannot be registered)
- `[x]` todo:1876 — **G4.2 — graph wiring** · **S** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1878 — Thin wiring over G2's shared skeleton; no new control flow
- `[x]` todo:1879 — ⛔ A `blocked` demon **generates nothing** — an answer, not a failure
- `[x]` todo:1880 — ⛔ An **unprefixed** id (`wallnut` not `commander-effect.wallnut`) **fails corpus load** —
- `[x]` todo:1882 — Re-run over unchanged input → **zero** new writes (G2 idempotence)
- `[x]` todo:1883 — Same corpus generated twice against the mock → byte-identical
- `[x]` todo:1886 — **G4.3 — real run + quality sample** · **M** ✅ **BUILT + VERIFIED 2026-09-01**
- `[x]` todo:1888 — ⛔ **Only after G1 has landed** — generating from `一类`/`僵尸` motifs would bake stat
- `[x]` todo:1890 — Every non-blocked demon has a commander effect; committed
- `[x]` todo:1891 — `Coverage/DemonUncovered` count **falls** by that number
- `[x]` todo:1892 — ⛔ **Quality reported from a read stratified sample**, never from the tier-2 pass rate
- `[x]` todo:1893 — If shoehorning **persists** after G1, that is the trigger to build CoVe — record the

### Part 5 — Feature 3: generation runtime (G0–G4) > Phase G4 — commander-effect (the first real generator) > ✅ CP-G4 — closes Part 5 — **REACHED 2026-09-01**  _(todo)_
- `[x]` todo:1899 — Full seedsmith suite green — **467 passed** (clean venv); four guard scripts green
- `[x]` todo:1900 — `Coverage/DemonUncovered` reduced — **84 gaps → 0**, verified by a real `check` run against
- `[x]` todo:1902 — Quality reported from a **read sample**, separately from the pass rate — and reading is what
- `[x]` todo:1904 — CoVe build decision recorded — **still not built, and the run reinforced that.** Both real

### Cross-part final verification — 2026-09-01 > ⛔ Defect found by this sweep (not by any part's own gate) — FIXED  _(todo)_
- `[x]` todo:1969 — **X1 — Prove the fix is safe before doing it.** `themes.v1.json` is append-only
- `[x]` todo:1975 — **X2 — Commit the regeneration entrypoint.** `seedsmith/adapters/demons/generate_themes.py`,
- `[x]` todo:1979 — **X3 — Rebuild and verify.** `--rebuild` → `{"themes": 84, "retired": 0}`; 0 stale vs
- `[x]` todo:1982 — **X4 — Pin it with a test, since no metric compared the two artifacts.**

### Cross-part final verification — 2026-09-01 > Sweep result  _(todo)_
- `[x]` todo:1989 — **X5 — Swept every other derived/derived-from pair in the demon tree for the same class.**
- `[x]` todo:1998 — seedsmith Python suite: **471 passed** (467 before this sweep; +2 from X4, +2 from X5).
- `[x]` todo:1999 — Theme staleness: **0 / 84**.
- `[x]` todo:2000 — Rebuild determinism: byte-identical across two runs.
- `[x]` todo:2001 — C# suites re-run rather than argued — Core **4896**, Data **548**, Guard **142**, all green.

### ⛔ The `l`-tag hole — found 2026-09-01 while proving CP-G1, fixed end to end > What was done  _(todo)_
- `[x]` todo:2043 — **L1 — Fix the filter.** Removed `l` from `_CONTENT_POS`; kept `i` (成语 — `人心惶惶`,
- `[x]` todo:2046 — **L2 — Measure the cost honestly.** Dropping `l` also loses 2 real motifs, `无人机` ("drone")
- `[x]` todo:2051 — **L3 — Regenerate motifs.** 9 of 84 demons changed, all improvements
- `[x]` todo:2055 — **L4 — The theme test proved itself.** Changing motifs made `themes.v1.json` stale again, and
- `[x]` todo:2059 — **L5 — Build the idempotency CP-G4 always required.** `Re-run produces zero new writes` was an
- `[x]` todo:2065 — **L6 — Regenerate only what was stale.** `--stale` → **9 generated, 75 kept, 0 escalated,
- `[x]` todo:2067 — **L7 — Verify by reading, not by pass rate.** All 9 read individually; 0 connectives remain
- `[x]` todo:2069 — **L8 — Cover it three ways**, since one test would only have caught one layer:

### Final-proof pass — 2026-09-01: reading the audit, not counting its boxes > Item 2 — the coverage gap D2.3 flagged in its own prose ✅ CLOSED  _(todo)_
- `[x]` todo:2139 — Two tests now pin the rule from **both** sides, and each was falsified independently:
- `[x]` todo:2145 — Two fixture traps recorded, because either yields a test that passes while proving nothing: a

### Final-proof pass — 2026-09-01: reading the audit, not counting its boxes > Item 3 — 6 live `SemanticDedup/NearDuplicate` GAPs, recorded but never closed ✅ CLOSED  _(todo)_
- `[x]` todo:2164 — Added `name_collision` (tier 2, deterministic, zero model cost — "cheapest instrument first").
- `[x]` todo:2167 — Regenerated exactly the 6 second-of-pair demons via `--only`, keeping the other 78 untouched
- `[x]` todo:2170 — **84 entries, 84 distinct names, 0 duplicates.** `seedsmith check` on the demons corpus went
- `[x]` todo:2173 — Covered six ways, including a corpus-level regression test — the metric is `gates=False`, and

### Full-audit re-read + evidence map — 2026-09-01 > ⛔ Three Verify lines were wrong, and only executing them could show it  _(todo)_
- `[x]` todo:2222 — **`python -m seedsmith demons motifs`** (G1.3) — **the command did not exist.** The CLI had
- `[x]` todo:2225 — **`python -m seedsmith demons generate --kind commander-effect`** (G4.3) — same, for the
- `[x]` todo:2227 — **`python -m seedsmith check --adapter items --metric Coverage/EmptyPartition`** (S2) — omitted
- `[x]` todo:2238 — Both G1.3 and G4.3 Verify lines now execute **exactly as the audit writes them**.
- `[x]` todo:2241 — `--kind aspect` **refuses** with exit 2 rather than silently generating nothing — `aspect` is
- `[x]` todo:2243 — ⛔ **Imports are deferred inside `cmd_demons`.** `generate` pulls in the workflow package and
- `[x]` todo:2248 — Proven on a genuinely engine-free install: in `.venv-base` (base deps only, `langgraph` absent)
- `[x]` todo:2250 — S2's Verify line corrected to include its corpus root.

