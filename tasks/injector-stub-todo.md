# Injector stub removal — tasks

Plan: [injector-stub-plan.md](injector-stub-plan.md)

- [x] W1 — Split `SecondaryPluginRegistry` default vs prove; add `RegisterById`
- [x] W2 — Update Core tests (lifecycle, host, funnel, offline kit)
- [x] W2 — Update scenario fixtures + optional `plugins` field on scenario DTO
- [x] W3 — Run Core + Guard tests; guard-funnel-delta
- [ ] W3 — Owner live spot-check: `deploy-play.ps1 -NoServer`, vanilla hit → no butter
