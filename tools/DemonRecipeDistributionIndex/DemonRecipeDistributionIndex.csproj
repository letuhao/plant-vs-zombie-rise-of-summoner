<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>FusionRpg.Tools.DemonRecipeDistributionIndex</RootNamespace>
    <AssemblyName>DemonRecipeDistributionIndex</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <!-- Re-derives concrete species from the real anchors (SpeciesExpander — the same method
         species-import already uses), imports them into a throwaway temp-directory RpgStore purely
         as the computation vehicle BuildDemonSpeciesSnapshot() already needs (never a real/installed
         save — the same "use a real store, discard it" pattern DemonSpeciesImport's own diff-catalog
         mode already established), then reuses DemonRecipeCatalog's own real search
         (NearestPopulatedRungBelow/OutputEligibilityFloor) rather than re-implementing "which rung is
         below" a second time. -->
    <ProjectReference Include="..\..\src\FusionRpg.Core\FusionRpg.Core.csproj" />
    <ProjectReference Include="..\..\src\FusionRpg.Data\FusionRpg.Data.csproj" />
  </ItemGroup>
</Project>
