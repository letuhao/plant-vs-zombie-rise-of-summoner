"""seedsmith — deterministic measurement + generation substrate for seed corpora.

Nothing beyond `pipeline.llm_caller` exists yet (tasks/seedsmith-todo.md, S0). `corpus`,
`adapters`, `metrics` and `report` land in later tasks per tasks/seedsmith-plan.md.
"""
