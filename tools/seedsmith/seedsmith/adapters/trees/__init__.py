"""seedsmith.adapters.trees — the passive-tree feature.

Foundation-only today (task A2's `targets.py`). `TreesAdapter`, `plan.py` (B1), `nodegen/` (H1-H3)
and the rest of the twelve `setgen`-mould modules `spec-tree-language.md` describes land as their
own tasks build them — this package exists now so `targets.py` has a home ahead of them, the same
"foundation before the consumer" shape A1's `PassiveTreeTuning.cs` used in `src/`.
"""
from __future__ import annotations
