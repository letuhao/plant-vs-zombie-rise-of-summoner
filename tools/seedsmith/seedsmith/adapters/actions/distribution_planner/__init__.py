"""seedsmith.adapters.actions.distribution_planner — A-S1 "Engine 1"
(spec-distribution-planner.md). Pure derivation package; the one file that touches disk is the
sibling entrypoint `../generate_distribution_planner.py`, matching every other `generate_*.py`
feature in this adapter family."""
