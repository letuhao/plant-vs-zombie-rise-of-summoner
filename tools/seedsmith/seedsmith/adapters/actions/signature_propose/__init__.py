"""seedsmith.adapters.actions.signature_propose --- A-P3 "signature-propose"
(spec-signature-propose.md, action-corpus program). Pure prompt/schema/derivation package; the one
file that touches disk or a model is the sibling entrypoint `../generate_signature_actions.py`,
matching every other `generate_*.py` feature in this adapter family (`family_propose`'s own
`__init__.py` states the identical split)."""
