"""seedsmith.adapters.actions.family_propose --- A-P2 "family-propose"
(spec-family-propose.md, action-corpus program). Pure prompt/schema/derivation package; the one
file that touches disk or a model is the sibling entrypoint `../generate_family_actions.py`,
matching every other `generate_*.py` feature in this adapter family (`general_propose`'s own
`__init__.py` states the identical split)."""
