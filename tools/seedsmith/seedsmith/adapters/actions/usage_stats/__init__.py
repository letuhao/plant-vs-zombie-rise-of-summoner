"""seedsmith.adapters.actions.usage_stats — FC1 `usage-stats` (roster-balance program).

Measures real per-family usage across every committed action-corpus round, over the 98 authored
atom/affix families. See `derive.py`'s own module docstring and
`docs/architecture/roster-balance/spec-usage-stats.md` for the full design.
"""
from __future__ import annotations
