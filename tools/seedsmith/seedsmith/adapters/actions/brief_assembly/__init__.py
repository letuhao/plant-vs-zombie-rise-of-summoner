"""seedsmith.adapters.actions.brief_assembly — A-S2 (spec-brief-assembly.md), the mechanical stage
that assembles A-P3's brief: A-S1's own species-scope (signature) brief entry, untouched, plus the
one field no earlier module owns -- `familyActions`, which cannot exist before A-P2's round has
been generated, validated (A-S4), deduped (A-S3) and id-assigned. Model-free; makes no judgement.
"""
from __future__ import annotations
