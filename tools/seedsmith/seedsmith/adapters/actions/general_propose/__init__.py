"""seedsmith.adapters.actions.general_propose --- A-P1 "general-propose"
(spec-general-propose.md, action-corpus program). Pure prompt/schema/derivation package; the one
file that touches disk or a model is the sibling entrypoint `../generate_general_actions.py`,
matching every other `generate_*.py` feature in this adapter family (`distribution_planner`'s own
`__init__.py` states the identical split)."""
